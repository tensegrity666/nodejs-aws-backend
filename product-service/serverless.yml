service:
  name: product-service
app: aws-fullstack-shop
frameworkVersion: '2'

plugins:
  - serverless-webpack
  # - serverless-dotenv-plugin

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: eu-west-1

environment:
  PG_HOST: rss-aws-shop.csk96ler9jqa.eu-west-1.rds.amazonaws.com
  PG_PORT: 5432
  PG_DATABASE: rss_postgress
  PG_USERNAME: RssPostgress
  PG_PASSWORD: iN6Q3pEFvpeK5qR

functions:
  getProductsListFromMock:
    handler: src/handler.getProductsListFromMock
    events:
      - http:
          path: product
          method: get
          cors: true
  getProductByIdFromMock:
    handler: src/handler.getProductByIdFromMock
    events:
      - http:
          path: product/{id}
          method: get
          cors: true
          request:
            parameters:
              paths:
                id: true
  invoke:
    handler: src/handler.invoke
